import { observable } from 'mobx';

// import { Model } from '../models/Model.jsx';

export default class MainStore {

	// @observable Model = null;

	constructor() {
		// this.Model = new Model(this, {/* initial values */})
	}

	// // or
	// @action createModelInstance(/* initial values */) {
	// 	this.model = new Model(this, {/* initial values */})
	// }
}